# ? ignore server
apps/server/

# ? yarn
.yarn/*
!.yarn/patches/
!.yarn/plugins/
!.yarn/releases/
!.yarn/sdks/
!.yarn/versions/
!.yarnrc.yml

# ? node
node_modules/

# ? ts build
*.tsbuildinfo

# ? turbo
turbo/
.turbo/

# ? build app
dist/
build/

# ? loggers
logger/

# ? client
apps/*/out/

# ? assets
uploads/